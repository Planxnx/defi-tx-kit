export enum Decimals {
	WEI = 18,
	GWEI = 9,
}

export enum TxSwapSide {
	BUY = "BUY",
	SELL = "SELL",
}

export enum PairEvent {
	SWAP = "Swap",
	SYNC = "Sync",
	MINT = "Mint",
	BURN = "Burn",
}

export enum TokenEvent {
	TRANSFER = "Transfer",
	APPROVAL = "Approval",
}
